{
  "//": "Root tsconfig for editor/tooling convenience. Each package and app has its own tsconfig that extends packages/config/tsconfig.base.json. Next.js consumes workspace packages via transpilePackages, so we do NOT use composite project references here.",
  "extends": "./packages/config/tsconfig.base.json",
  "include": [],
  "exclude": ["node_modules", "**/dist", "**/.next"]
}
