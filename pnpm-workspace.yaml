packages:
  - "."

# Build-script approvals: esbuild's postinstall just verifies its prebuilt
# binary; canvas (optional dep of pdfjs via unpdf) would compile from source
# and isn't needed in a webview.
allowBuilds:
  esbuild: true
  canvas: false
